---
notification: patch
notification-js: patch
---

The `visibility` and `number` notification options are now applied on Android. They were dropped before reaching the Android plugin, and `number` was not implemented.
