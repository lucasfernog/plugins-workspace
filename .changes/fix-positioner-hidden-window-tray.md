---
positioner: patch
positioner-js: patch
---

Moving a window to a `Tray*` position no longer requires the window to be on a monitor, so a hidden window can be positioned next to the tray icon before it is shown. The window size is now read once per move.
