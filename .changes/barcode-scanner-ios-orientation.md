---
barcode-scanner: patch
barcode-scanner-js: patch
---

On iOS, the camera preview now defaults to portrait instead of upside down when the interface orientation is unknown, and an unexpected metadata object no longer crashes the app.
