---
updater: patch
updater-js: patch
---

On macOS, install updates for apps that live on a different volume than the system temporary directory (for example an external disk). The update is now staged next to the app in that case, and the admin prompt is used as a fallback when that is not possible.
