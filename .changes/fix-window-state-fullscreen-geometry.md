---
window-state: patch
window-state-js: patch
---

The size and position of a fullscreen window are no longer saved as its normal geometry, so a window restored to fullscreen gets its previous size back when it leaves fullscreen instead of staying monitor-sized.
