---
opener: minor
opener-js: minor
---

Added `revealItemsInDir(paths: string[])`, the plural counterpart of `revealItemInDir` matching the Rust `reveal_items_in_dir` API, so apps can switch to it ahead of the planned rename of the command.
