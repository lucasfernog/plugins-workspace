---
updater: patch
updater-js: patch
---

On macOS, return an error instead of panicking when the AppleScript used to install an update with administrator privileges fails to compile, or cannot be run on the main thread.
