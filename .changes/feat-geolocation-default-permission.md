---
geolocation: minor
geolocation-js: minor
---

Added a `geolocation:default` permission set that allows every geolocation command: `check-permissions`, `request-permissions`, `get-current-position`, `watch-position` and `clear-watch`. Nothing is granted unless a capability lists it.
