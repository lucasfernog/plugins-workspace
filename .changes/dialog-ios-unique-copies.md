---
dialog: patch
dialog-js: patch
---

On iOS, photos and videos picked through the media picker are now copied into a unique folder inside the app's caches directory, so picked files with the same name no longer overwrite or delete each other or a file returned by an earlier pick.
