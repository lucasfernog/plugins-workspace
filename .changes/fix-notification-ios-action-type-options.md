---
notification: patch
notification-js: patch
---

Fixed iOS action types: `hiddenPreviewsBodyPlaceholder` is now applied, and the action (`foreground`, `destructive`, `requiresAuthentication`) and action type (`customDismissAction`, `allowInCarPlay`, `hiddenPreviewsShowTitle`, `hiddenPreviewsShowSubtitle`) flags are now combined instead of only the first one set being used.
