---
http: patch
http-js: patch
---

`fetch` no longer modifies the options object it is given: it used to delete the `maxRedirections`, `connectTimeout`, `proxy` and `danger` options from it, and to write the headers generated by the browser (such as a multipart `content-type` boundary) into a `Headers` object passed as `headers`, which broke reusing the options for another request.
