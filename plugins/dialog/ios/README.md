# Tauri Plugin Dialog

The iOS (Swift) implementation of [`tauri-plugin-dialog`](../README.md): file, save and message dialogs
backed by `UIDocumentPickerViewController`, `PHPickerViewController` (`UIImagePickerController` on
iOS 13) and `UIAlertController`. It is built by the plugin's Rust crate and is not meant to be used
on its own.
