<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <!-- shares local files with the app that `openPath` opens them with -->
    <provider
      android:name="app.tauri.opener.OpenerFileProvider"
      android:authorities="${applicationId}.opener.fileprovider"
      android:exported="false"
      android:grantUriPermissions="true">
      <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/tauri_opener_file_paths" />
    </provider>
  </application>
</manifest>
