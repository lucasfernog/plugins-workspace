{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions to run the example",
  "windows": ["main"],
  "permissions": ["core:default", "websocket:default"]
}
